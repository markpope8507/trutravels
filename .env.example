# Ask Tru.D (src/app/api/trud/route.ts). Get a key from console.anthropic.com
# and set it here for local dev and in Vercel > Project > Settings > Environment Variables.
ANTHROPIC_API_KEY=

# Optional: override the Google Sheet the knowledge sync script reads.
# TRUD_SHEET_ID=
